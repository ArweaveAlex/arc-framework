import { PoolType } from '../helpers/types';
export declare function sortByAll(pools: PoolType[], amount: number | null): PoolType[];
export declare function sortByNewest(pools: PoolType[], amount: number | null): PoolType[];
export declare function sortByMostContributed(pools: PoolType[], amount: number | null): PoolType[];
